package com.cobiscorp.cobis.busin.flcre.commons.utiles;

public class Compare {

	public static boolean ForString(String originalValue, String compareValue) {
		return (originalValue != null && originalValue.equals(compareValue));
	}

}
